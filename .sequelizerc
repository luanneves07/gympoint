/**
 *  Este arquivo serve para exportar o caminho ate os arquivos e diretorios criados.
 * Necessario utilizar a sintaxe antes do import/export pois ele ainda nao suporta
 * a nova sintaxe.
 */

 // Pacote necessario para padronizar caminhos tanto em windows, linux, mac, etc
 const { resolve } = require('path');


module.exports = {
  // Caminho até o diretório de configurações do banco de dados
  config: resolve(__dirname, 'src', 'config', 'database.js'),
  // Caminho até o diretório de models do projeto
  'models-path': resolve(__dirname, 'src', 'app', 'models'),
  // Caminho até o diretório de controllers do projeto
  'migrations-path':resolve(__dirname, 'src', 'database', 'migrations'),
  // Caminho até o diretório de seeds do projeto
  'seeders-path':resolve(__dirname, 'src', 'database', 'seeds'),
}
